@inject IJSRuntime IJSRuntime
<h3 class="text-dark">Child Component</h3>
<p class="text-dark pl-2"> There is a message - @MessageForChild </p>

@*<div class="alert alert-info">@Title</div>
    @if (NewChildContent == null)
    {
        @if (ChildContent == null)
        {
            <span>Here is Empty Render Fragment</span>
        }
        else
        {
            <span>@ChildContent</span>
        }
    }
    else
    {
        <span>@NewChildContent</span>
    }*@

<GrandChildComponent>

</GrandChildComponent>

@code {
    [Parameter]
    public string Title { get; set; }
    [Parameter]
    public RenderFragment ChildContent { get; set; }
    [Parameter]
    public RenderFragment NewChildContent { get; set; }
    [CascadingParameter(Name = "message")]
    public string MessageForChild { get; set; }

    public async Task DisplayToastrSuccess()
    {
        await IJSRuntime.ToastrSuccess("This toastr come from child");
    }
}
